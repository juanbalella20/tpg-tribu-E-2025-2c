# tpg-tribu-E-2025-2c

en la carpeta `backend/horas/` para correr la API
```
pip install -r requirements.txt --break-system-packages
```
y para correr React en debemos estar en la carpeta `frontend/`
```
npm install
npm run dev
```